#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

Функция ПолучитьТаблицуИнформационныхСсылокДляВиджета() Экспорт
	
	ИнформационныеСсылкиXML = Обработки.МетодическаяПоддержка.ПолучитьМакет("ИнформационныеСсылки").ПолучитьТекст();
	ТаблицаСсылок = ОбщегоНазначения.ПрочитатьXMLВТаблицу(ИнформационныеСсылкиXML).Данные;
	РольПользователя = "Бухгалтер";
	
	Строки = ТаблицаСсылок.НайтиСтроки(Новый Структура("Роль", РольПользователя));
	ТаблицаРезультата = ТаблицаСсылок.Скопировать(Строки);
	ТаблицаРезультата.Сортировать("Вес");
	
	Возврат ТаблицаРезультата;
	
КонецФункции

Функция ПолучитьИмяСобытияДляЖурналаРегистрации() Экспорт
	
	Возврат НСтр("ru = 'Методическая поддержка'", ОбщегоНазначенияКлиентСервер.КодОсновногоЯзыка());
	
КонецФункции

#КонецОбласти

#КонецЕсли